// Place all the styles related to the StaticPages controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

#background-overlay {
	position: fixed;
	background: url(<%= asset_path "mattbackground.jpg"%>) no-repeat fixed;
	background-position: 70% center;
	background-size: cover;
	min-height: 100vh;
	width: 100%;
	z-index: -101;
}

li {
	font-size:1.25em;
}

video#bgvideo { 
    position: fixed;
    top: 50%;
    left: 50%;
    min-width: 100%;
    min-height: 100%;
    width: auto;
    height: auto;
    z-index: -100;
    -ms-transform: translateX(-50%) translateY(-50%);
    -moz-transform: translateX(-50%) translateY(-50%);
    -webkit-transform: translateX(-50%) translateY(-50%);
    transform: translateX(-50%) translateY(-50%);
    background: url(<%= asset_path "videobackground.png"%>) no-repeat;
    background-size: cover; 
}

// Hide the background video for most phones
@media screen and (max-device-width: 800px) {
    html {
         background: url(<%= asset_path "mattbackground.jpg"%>) #000 no-repeat center center fixed;
    }
    #bgvideo {
        display: none;
    }
    li { 
        font-size:2.25em;
    }
}